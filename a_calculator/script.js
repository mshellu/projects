const numberButtons = document.querySelectorAll('[data-number]')
// document method querySelectorAll(): returns static NodeList (Node is an interface including Document)
// static NodeList contains all elements 